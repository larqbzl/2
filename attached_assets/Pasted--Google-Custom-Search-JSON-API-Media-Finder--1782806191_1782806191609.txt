«Добавь новый источник изображений — Google Custom Search JSON API — в Media Finder. Делай это поэтапно, и после каждого этапа сначала протестируй сам (через curl/логи сервера), покажи мне результат теста, и только после моего подтверждения переходи к следующему этапу. Не объявляй этап завершённым, пока не проверил его вручную, а не просто написал код.
ЭТАП 1 — Ключ и базовый прокси

Добавь в Settings два новых поля рядом с Pexels/Pixabay: Google API Key и Search Engine ID (cx). Та же архитектура: ключи хранятся только на сервере в artifacts/api-server/data/api-keys.json, никогда не уходят в браузер. Кнопки Save/Test/Clear, статус "Saved"/"Not set", как у существующих полей.
Над полями добавь короткую подсказку со ссылками: где создать API key (Google Cloud Console → Custom Search API) и где создать Search Engine ID (cse.google.com, с опцией "Search the entire web").
Создай серверный эндпоинт GET /api/proxy/google-images — принимает query и count, обращается к https://www.googleapis.com/customsearch/v1 с параметрами key, cx, searchType=image, q. Используй ту же безопасную обработку ответа, что и для Pixabay — сначала читать как текст, потом пытаться распарсить JSON, не падать на ошибках Google.
Создай GET /api/proxy/google-images/test для кнопки Test в Settings (один тестовый запрос с минимальным count).
Подключи Google Images как пятый источник на странице Search, в одну линию с остальными.
После того как сделаешь — протестируй сам: вызови /api/proxy/google-images/test через curl с реальным ключом и покажи мне сырой ответ. Затем сделай обычный поисковый запрос через интерфейс (например "Protocol Index store") и покажи, что в выдаче реально релевантные картинки, а не случайные совпадения по словам.

ЭТАП 2 — Batch с построчным списком запросов (делай только после моего подтверждения по Этапу 1)

В Batch добавь textarea, куда можно вставить список поисковых запросов построчно (один запрос — одна строка).
Сайт должен прогонять каждую строку как отдельный запрос по всем подключённым источникам разом (Archive.org, Pexels, Pixabay, Wikimedia, Google Images).
Покажи прогресс выполнения в реальном времени (например, "12 / 40 запросов обработано"), чтобы было видно, что процесс не завис.
Учти лимит Google API — 100 бесплатных запросов в день. Покажи счётчик использованных запросов за сегодня в интерфейсе (если Google отдаёт remaining quota в заголовках ответа — используй его, если нет — веди свой счётчик на сервере с сбросом раз в сутки) и предупреждай, если батч из N строк может этот лимит превысить, прежде чем его запускать.
Протестируй сам на 5-10 строках с разными запросами и покажи мне, что все обработались, ничего не потерялось и счётчик квоты обновился корректно.

ЭТАП 3 — Массовое скачивание (делай только после моего подтверждения по Этапу 2)

В результатах Batch добавь чекбоксы для выбора нужных картинок/видео и кнопку "Download selected".
Выбранные файлы должны скачиваться одним zip-архивом.
Имя каждого файла внутри архива должно включать источник и часть поискового запроса (например google_protocol-index-store-opening_01.jpg), чтобы было понятно, откуда что взялось.
Протестируй сам: выбери несколько результатов из разных источников, скачай архив, открой и проверь, что все файлы на месте, не битые и с понятными именами. Покажи мне результат.

После каждого этапа жди моего "ок, дальше" перед тем как начинать следующий — не делай все три этапа за один проход.»